-- filter the data using where and having clause --
-- where clause apply filter row by row 

-- average salary of department > 10,000 in that case we use group by and then having clause
-- aggregated values we use the having clause

-- where filter is applied first and then having filter


-- Query Process Steps

-- 1. Getting Data (From, Join)

-- 2. Row Filter (Where)

-- 3. Grouping (Group by)

-- 4. Group Filter (Having)

-- 5. Return Expressions (Select)

-- 6. Order & Paging (Order by & Limit / Offset)


